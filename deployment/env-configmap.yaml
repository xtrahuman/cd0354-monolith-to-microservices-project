apiVersion: v1
kind: ConfigMap
metadata:
  name: data-config
data:
  # property-like keys; each key maps to a simple value
  postgres_host: "microservicedata.cqxx3wwfyjmy.us-east-1.rds.amazonaws.com"
  postgres_db: "microservicedata"
  aws_bucket: "myawsmyaws740127263396bucket"
  aws_region: "us-east-1"
  aws_profile: "udacityLabs"
  url: "http://localhost:8080"